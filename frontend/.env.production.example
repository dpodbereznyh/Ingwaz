# Copy to frontend/.env.production before npm run build (not committed).

VITE_SITE_URL=https://ingwaz.space

# Dedicated counter for ingwaz.space (loads on SPA start; banner records acknowledgment)
VITE_YANDEX_METRIKA_ID=109383201

# Open Graph / Twitter preview (absolute URL; 1200×630 recommended — см. public/og-image.svg)
VITE_OG_IMAGE_URL=https://ingwaz.space/og-image.svg
